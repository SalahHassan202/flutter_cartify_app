import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFF2563EB);
  static const Color accent = Color(0xFFF48C25);

  // Backgrounds
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color darkBackground = Color(0xFF0D131C);

  // Text Colors
  static const Color textPrimary = Color(0xFF0D131C);
  static const Color textSecondary = Color(0xFF4B5563);
  static const Color textLight = Color(0xFFFFFFFF);

  // Extra Surface
  static const Color surfaceDark = Color(0xFF1C140D);

  // Buttons
  static const Color buttonPrimary = primary;
  static const Color buttonText = lightBackground;

  // Status
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
}
